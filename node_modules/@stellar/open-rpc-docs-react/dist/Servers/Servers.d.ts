import React, { Component } from "react";
import { ServerObject } from "@open-rpc/meta-schema";
interface IProps {
    servers?: ServerObject[];
    uiSchema?: any;
    components?: {
        CodeBlock: React.FC<{
            children: string;
            className?: string;
        }>;
    };
    noTitle?: boolean;
}
declare class Servers extends Component<IProps> {
    render(): React.JSX.Element | null;
}
export default Servers;
